name: abstract_bloc
description: Abstraction for fetching local or remote data via flutter_bloc
version: 1.2.32
repository: https://github.com/salihagic/abstract_bloc

environment:
  sdk: '>=3.0.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.4
  rest_api_client: ^2.2.17
  pull_to_refresh: ^2.0.0
  provider: ^6.1.2
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
