name: abstract_bloc
description: Abstraction for reading and persisting data to platform specific storage.
version: 0.0.3
repository: https://github.com/salihagic/abstract_bloc

environment:
  sdk: ">=2.12.1 <3.0.0"
  flutter: ">=2.0.2"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.1
  rest_api_client: ^2.0.0
  pull_to_refresh: ^2.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
