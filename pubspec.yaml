name: abstract_bloc
description: Abstraction for fetching local or remote data via flutter_bloc
version: 1.4.1
repository: https://github.com/salihagic/abstract_bloc

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.0
  flutter_bloc_event_bus: ^0.0.3
  rest_api_client: ^2.3.7
  pull_to_refresh: ^2.0.0
  provider: ^6.1.2