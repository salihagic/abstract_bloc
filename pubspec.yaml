name: abstract_bloc
description: Abstraction for fetching local or remote data via flutter_bloc
version: 1.4.14
repository: https://github.com/salihagic/abstract_bloc

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^9.1.1
  flutter_bloc_event_bus: ^0.0.5
  rest_api_client: ^2.3.13
  pull_to_refresh: ^2.0.0
  provider: ^6.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0